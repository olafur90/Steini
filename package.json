{
  "name": "steini",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browser-sync": "browser-sync start --server --files index.html sites/*.html styles.css",
    "sass": "node-sass styles/styles.scss styles.css",
    "sass-watch": "node-sass --source-map-embed --watch styles/styles.scss --recursive styles.css",
    "dev": "concurrently npm:browser-sync npm:sass-watch npm:node-sass npm:sass"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.13",
    "concurrently": "^5.3.0",
    "node-sass": "^5.0.0",
    "sass": "^1.30.0",
    "sass-watch": "^0.1.0"
  }
}
